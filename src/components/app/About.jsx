import SectionHeading from "../common/SectionHeading";

const About = () => {
  return (
    <section
      id="about"
      className="w-full h-[100vh] text-[var(--secondaryColor)]"
    >
      <SectionHeading title="About" highlight="Me!" />
    </section>
  );
};

export default About;
